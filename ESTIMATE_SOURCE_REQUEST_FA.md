# درخواست تغییر منبع «برآورد اولیه» به فایل مبنا

**از:** تیم Frontend مالی · **به:** تیم Backend مالی
**نوع:** تصمیم محصولی مالک پروژه + درخواست پیاده‌سازی
**تاریخ:** ۱۴۰۵/۰۶/۱۷

---

## ۱. تصمیم مالک پروژه

**برآورد اولیه باید از فایل خوانده شود، نه محاسبه.** اولویت مطلق با فایلی است که در
سایت آپلود و در دیتابیس ذخیره شده است.

> «اصل پروژه همین است که برآوردها باید از فایل‌ها و فایل‌های دوره‌ای و فایل مبنا گرفته
> شود و اگر نبود آن‌وقت ما بسازیم.»

این تصمیم، تصمیم قبلی (۱۴۰۵/۰۶/۰۵ — «فایل هیچ هزینه‌ای ندارد، پول در ماژول ساخته
می‌شود») را **صریحاً نقض و جایگزین می‌کند**. مالک این را با آگاهی از تصمیم قبلی گفته است.

---

## ۲. مدل داده‌ای که باید پیاده شود

```
پروژه  =  مجموع برآورد اولیهٔ فعالیت‌ها
   │
   └── هر فعالیت، دقیقاً یکی از این دو مسیر:
         الف) مبلغ برآورد مستقیماً در فایل مبنا آمده   →  همان عدد، بدون تغییر
         ب)  فایل فقط مقدار/تعداد اقلام را داده        →  Σ (مقدار × قیمت واحد)
```

### قواعد قطعی

1. **هرگز هر دو مسیر با هم.** اگر فعالیتی هم مبلغ مستقیم دارد و هم اقلام قیمت‌دار،
   فقط مسیر «الف» اعمال شود. اجرای هر دو یعنی چندبرابر شدن عدد — این یک باگ است، نه
   یک انتخاب.
2. **برآورد اولیه هرگز اصلاح نمی‌شود.** یک‌بار در ابتدای پروژه تعیین می‌شود و ثابت
   می‌ماند. مفهوم «برآورد اصلاح‌شده» از محصول حذف می‌شود (جزئیات در بند ۶).
3. **مبنا فقط فایل مبناست، نه فایل دوره‌ای.** اگر برآورد اولیه از آخرین فایل دوره‌ای
   خوانده شود، با هر آپلود تغییر می‌کند و دیگر «اولیه» نیست — و انحراف از برنامه
   بی‌معنا می‌شود، چون برنامه همراه واقعیت جابه‌جا شده است.
4. **نبودِ عدد، صفر نیست.** فعالیتی که نه مبلغ فایل دارد و نه قیمت قابل محاسبه،
   `null` می‌گیرد نه `0` — همان قاعده‌ای که ماژول امروز برای مقدار اجراشده دارد.

---

## ۳. آنچه ما در کد شما پیدا کردیم (برای صرفه‌جویی در وقت شما)

مخزن را خواندیم. **زیرساخت لازم تقریباً کامل موجود است** و migration جدید لازم نیست.

| قطعه | محل | وضعیت |
|---|---|---|
| هزینهٔ فایل در سطح **تسک** | `coreint/mpp_reader.py:284` — `task.getCost()` | ✅ خوانده می‌شود |
| هزینهٔ فایل در سطح **تخصیص** (تسک × منبع) | `coreint/mpp_reader.py:415` — `assignment.getCost()` | ✅ خوانده می‌شود |
| ذخیرهٔ هزینهٔ تخصیص | `coreint/mpp_import.py:451` → `msp_resource_assignments.source_cost` | ✅ ذخیره می‌شود |
| پیوند خط برآورد به سطر فایل | `alembic/0013` → `estimate_lines.source_assignment_uid` و `source_task_uid` (با ایندکس یکتا) | ✅ موجود |
| تک‌نقطهٔ محاسبهٔ برآورد | `app/finance/domain/reports.py:194` | یک خط |

یعنی زنجیرهٔ کامل از قبل هست:

```
estimate_lines.source_assignment_uid
      ↓
msp_resource_assignments.source_cost      ← مبلغ فایل
```

---

## ۴. باگی که همین حالا در کد هست و باید اصلاح شود

در `coreint/finance_mpp_sync.py`:

```python
# خط ۷۹ — داخل base(task):
"source_cost": _decimal(metrics.get("task_cost")),   # ← هزینهٔ کلِ تسک

# خط ۹۲–۹۳ — ردیف تخصیص:
row = base(task)          # هزینهٔ کل تسک را به ارث می‌برد
row.update({ "source_assignment_uid": ..., "source_resource_uid": ..., ... })
#            ↑ هزینهٔ خودِ تخصیص (assignment["cost"]) هرگز نوشته نمی‌شود
```

**نتیجه:** در جدول `finance_mpp_rows`، هزینهٔ کل یک فعالیت روی **تک‌تک** ردیف‌های
تخصیص آن تکرار می‌شود. فعالیتی با ۵ قلم، هزینه‌اش ۵ بار شمرده می‌شود.

مثال:

```
فعالیت «بتن‌ریزی فونداسیون» — هزینهٔ فایل: ۹۰۰ میلیون ریال
  ردیف سیمان       → source_cost = ۹۰۰  ✗  (باید ۳۰۰ باشد)
  ردیف میلگرد      → source_cost = ۹۰۰  ✗  (باید ۴۰۰ باشد)
  ردیف اجرت کارگر  → source_cost = ۹۰۰  ✗  (باید ۲۰۰ باشد)
                     جمع = ۲٬۷۰۰ به‌جای ۹۰۰
```

**این یک باگ است، نه یک تصمیم طراحی.** مالک صریحاً تأیید کرده که چندبرابرشدن اعداد
غیرقابل قبول است.

**دو راه اصلاح — لطفاً بگویید کدام را انتخاب می‌کنید:**

- **الف)** برای این کار از `msp_resource_assignments.source_cost` بخوانید (که هزینهٔ
  درستِ هر تخصیص را دارد) و `finance_mpp_rows.source_cost` را دست‌نخورده بگذارید.
- **ب)** یک ستون جدید `source_assignment_cost` به `finance_mpp_rows` اضافه کنید و
  `assignment["cost"]` را در آن بنویسید.

ما «الف» را ترجیح می‌دهیم چون داده از قبل ذخیره شده و migration نمی‌خواهد.

> ⚠ **هشدار جدی:** این باگ خاموش است. تا روزی که کسی جمع گزارش را با فایل مقایسه کند
> دیده نمی‌شود، و تا آن روز هر گزارش مالی صادرشده عدد غلط دارد.

---

## ۵. تغییری که لازم داریم

### ۵-۱. کوئری

`app/finance/repositories/reports.py:36` — به `SELECT` بزرگ، مبلغ فایل هر خط اضافه شود
(از `msp_resource_assignments` روی `source_assignment_uid`، و هزینهٔ تسک از مسیر
`source_task_uid`).

### ۵-۲. فرمول

`app/finance/domain/reports.py:194` امروز این است:

```python
initial = money(original_quantity * original_price)
```

باید نردبان اولویت شود، **در سطح فعالیت**:

```
اگر فعالیت مبلغ مستقیم در فایل دارد   →  همان مبلغ (و اقلامش مبلغ جداگانه نگیرند)
وگرنه                                 →  Σ (مقدار × قیمت) روی اقلام همان فعالیت
```

### ۵-۳. شفافیت منبع

هر عدد برآورد باید بگوید از کجا آمده. یک فیلد در پاسخ:

```
estimateSource: "file_task" | "file_assignment" | "computed" | "unavailable"
```

**چرا لازم است:** اگر بخشی از فعالیت‌ها از فایل و بخشی محاسباتی باشند، جمع کل ترکیبی
از دو منبع است. بدون این فیلد، کاربر نمی‌تواند جمع را توضیح دهد و ما نمی‌توانیم در UI
نشانش دهیم.

---

## ۶. «برآورد اصلاح‌شده» بازنشسته می‌شود

مالک: **«برآورد هیچ‌وقت نباید اصلاح شود. برآوردها همه همان اول تعیین می‌شود.»**

وضعیت فعلی: `POST /estimate-lines/{lineId}/revisions` و جدول `estimate_revisions` و
عدد `revisedEstimateIrr` وجود دارند.

**درخواست ما: حذف نکنید، فقط ارائه نکنید.**

- جدول `estimate_revisions` و رکوردهای موجود دست‌نخورده بمانند (append-only و ممیزی‌شده).
- `revisedEstimateIrr` می‌تواند در قرارداد بماند ولی برابر `initialEstimateIrr` شود، یا
  `null` برگردد — **لطفاً بگویید کدام را ترجیح می‌دهید** تا فرانت هماهنگ شود.
- endpoint بازنگری بماند ولی UI دیگر آن را صدا نمی‌زند.

دلیل: حذف برگشت‌ناپذیر است و رکوردهای ممیزی موجود نباید از بین بروند.

---

## ۷. پرسش‌های باز — لطفاً پاسخ دهید

این‌ها را نمی‌توانیم از کد استنباط کنیم:

1. **فایل مبنا چطور تشخیص داده می‌شود؟**
   ماژول امروز `snapshotType: ACTUAL | TARGET` دارد ولی مفهوم صریح «مبنا» ندارد. برآورد
   اولیه باید از فایل مبنا خوانده شود، نه آخرین فایل دوره‌ای. آیا ستون یا پرچمی برای
   این هست؟ اگر نیست، چه چیزی لازم دارید تا اضافه شود؟

2. **در فایل واقعی این پروژه، هزینه در کدام سطح آمده است؟**
   - سطح تسک (`task.getCost()`)؟
   - سطح تخصیص (`assignment.getCost()`)؟
   - هر دو؟
   - هیچ‌کدام و فقط مقدار؟

   لطفاً برای فایل مبنای واقعی این پروژه بشمارید: **چند تسک `Cost` غیرصفر دارند و چند
   تخصیص.** این عدد تعیین می‌کند کدام مسیر نردبان اولویت اصلاً فعال می‌شود.

3. **عدد ۳٬۶۰۵٬۸۲۳٬۹۶۷٬۷۷۵ که در نمونه فرستادید از کدام فایل و کدام سطح آمده؟**
   اگر جمع `task_cost` است، آیا با جمع `assignment_cost` می‌خواند یا دوباره‌شماری دارد؟

4. **اگر تسک هزینه دارد ولی مجموع تخصیص‌هایش با آن نمی‌خواند، کدام معتبر است؟**
   (در MSP معمولاً `task.Cost = Σ assignment.Cost + fixedCost`، ولی همیشه نه.)

5. **`fixed_cost` تسک چه می‌شود؟** آیا باید به برآورد اضافه شود یا جداست؟

6. **دو انبار موازی:** هم `finance_mpp_rows.source_cost` هست و هم
   `msp_resource_assignments.source_cost`، با معانی متفاوت. کدام مرجع رسمی است؟

---

## ۸. آنچه از شما می‌خواهیم برگردانید

```
□ پاسخ به شش پرسش بند ۷
□ تصمیم دربارهٔ راه اصلاح باگ بند ۴ (الف یا ب)
□ تصمیم دربارهٔ revisedEstimateIrr (بند ۶)
□ قرارداد نهایی: نام دقیق فیلدها، نوع‌ها، و مقدار در حالت نبودِ داده
□ اینکه این تغییر روی endpointهای موجود اعمال می‌شود یا endpoint جدیدی می‌آید
```

**نکتهٔ مهم دربارهٔ نقطهٔ تغییر:** لطفاً تغییر را در همان `domain/reports.py` انجام
دهید، نه در یک endpoint جداگانه. تمام گزارش‌های ماژول — زنده، `by-wbs`، ماهانه،
Snapshot، سطح ۱، دوره‌ای، گزارش‌ساز — از همان یک عدد مشتق می‌شوند. تغییر در آن نقطه
یعنی همه‌جا درست می‌شود؛ endpoint جداگانه یعنی دو عدد متفاوت در دو صفحه، که بدترین
حالت ممکن است.

---

## ۹. برآورد ما از حجم کار

| کار | تخمین |
|---|---|
| JOIN + نردبان اولویت + فیلد منبع | ۳–۴ ساعت |
| اصلاح باگ چندبرابرشدن | ۱–۲ ساعت |
| تشخیص فایل مبنا (اگر لازم شد) | ۴–۸ ساعت |
| تست‌های بک‌اند | ۲–۳ ساعت |

سمت ما (فرانت): حدود ۵–۷ ساعت. **مجموع: حدود ۲ تا ۳ روز کاری.**

فرانت ۴۵ نقطهٔ مصرف `initialEstimateIrr` دارد ولی هیچ‌کدام محاسبه نمی‌کنند — فقط
نمایش. پس تغییر معنای این عدد به ما آسیب نمی‌زند و منتظر قرارداد شما می‌مانیم.
