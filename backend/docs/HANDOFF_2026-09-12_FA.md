# تحویل کار — BAMBO Finance، تا ۱۴۰۵/۰۶/۲۱ (۲۰۲۶-۰۹-۱۲)

> این سند برای کسی نوشته شده که کار را از اینجا برمی‌دارد. هر ادعا شاهد اجرایی دارد و
> هرجا چیزی اثبات نشده، همان‌طور نوشته شده.

---

## ۱. در یک نگاه

| | |
|---|---|
| کاندیدا | برنچ `api-audit`، کامیت **`a86faaf4`**، پوش‌شده روی `origin` |
| نسخهٔ ارائه | `E:\bamboo\FINANCE` روی `5632c23d`، پورت ۸۰۴۰ — **در کل این کار لمس نشد** |
| سوئیت‌ها | بک‌اند **۱۲۲۸ passed** · فرانت **۴۷۹ passed** · چیدمان **۱۷۰ اجرا، صفر خطا** |
| قرارداد | `contracts/openapi.json` جاری، ۵۱ path |
| merge / deploy | **انجام نشد** و در اختیار این کار نبود |

**وضعیت انتشار: مسدود** — به دلیل ۷ مانع P0 که هیچ‌کدام کار کد نیست (بند ۵).
این با «پیشرفت صفر» یکی نیست: از ۹۱ الزام یکتای مرجع، ۶۱ بسته است.

---

## ۲. محیط‌ها

| پورت | دیتابیس | نقش | قاعده |
|---|---|---|---|
| **۸۰۴۰** | `bambo_canonical_test` | نسخهٔ ارائه | **محافظت‌شده.** ری‌استارت نشود، دیتابیسش نوشته نشود |
| **۸۰۴۲** | `bambo_apiaudit_20260910` | محیط ممیزی و شواهد | فقط‌خواندنی نگه داشته شد؛ اعداد شواهد به آن استناد دارند |
| **۸۰۴۵** | `bambo_authtest_20260912` | آزمون هویت‌ها | یک‌بارمصرف، چهار هویت ساختگی «آزمون: …» |

دیتابیس‌های یک‌بارمصرف که هر وقت لازم شد حذف می‌شوند:
`bambo_apiaudit_trial` (آزمون import) · `bambo_at23_pg16` (آزمون migration) · `bambo_authtest_20260912`.

**مفسر و JRE:** `E:/bamboo/FINANCE/backend/ai-extraction-env/Scripts/python.exe`
با `MPP_JAVA_HOME=E:/bamboo/jre/jdk-17.0.20.1+1-jre`.
بدون این متغیر، ۱۱ آزمونِ فایل واقعی MPP خطا می‌دهند — **پس‌رفت نیست، پیکربندی است.**

**فایل منبع MPP:** `E:\bamboo\FINANCE\Sources\test_progress.mpp`،
sha256 `12cd80770dfb4421dbe293b8c7194d17d7eedc7f756778e75d13a26917700664` — **تغییر نکرد**.

---

## ۳. کامیت‌ها روی `api-audit`

از `e3f0c715` تا `a86faaf4`، هفت کامیت:

| کامیت | چه کرد |
|---|---|
| `20e4a5cc` | سند شواهد: طبقه‌بندی WORK، تفکیک صفر از نامعلوم، اعتبار برآورد مصالح، مسیر گزارش دوره‌ای، مقایسهٔ نسخه‌ها، ۲۸۹ تکمیل، پنج جفت تخصیص، آزمایش import |
| `f0a9378d` | ثبت یک ردیف گم‌شده در دیتابیس کاری (بند ۴-ز) |
| `d2733cd3` | پرسش `sourceMethod` برای تیم Progress |
| `60f5307f` | **اصلاح:** خط فاکتور دیگر به رکورد soft-delete وصل نمی‌شود |
| `324b3862` | **آزمون:** سه چرخهٔ وضعیت واژگان هم را قرض نمی‌گیرند (AT-27 / Kit 05) |
| `2766ddc4` | **اصلاح:** گزارش چاپی دیگر هشت مرحله از سیزده مرحله را پنهان نمی‌کند |
| `a86faaf4` | **اصلاح:** بازگرداندن `finance.view` به درِ «گزارش مالی» + دو پیامد آن |

هیچ‌کدام روی `master` نرفت.

---

## ۴. آنچه اثبات شد

### الف) چهار ابهام منبع — همه بسته

- **طبقه‌بندی WORK:** نگاشت خودکار عمداً WORK را طبقه‌بندی نمی‌کند (`_finance_type` → `None`).
  هر ۱۸ منبع را یک انسان از مسیر `classify()` تعیین کرده و هر ۱۸ تا واقعاً ماشین‌آلات‌اند.
  «۴۲۶» تعداد **خط برآورد** بود نه منبع. **نقصی نبود.**
- **صفر در برابر نامعلوم:** فایل برای تجهیزات `Rate(0)` می‌دهد (نه null) ولی مقدار فیزیکی را
  واقعاً `null`. ستون `quantity` روی هر ۷۸۹ ردیف خالی است چون **۲۲ ستون سفارشی فایل هیچ‌کدام
  «مقدار» نیستند** — و ستون‌های «حجم انجام شده»/«حجم اولیه»/«احجام کاری» روی هر ۳۲۸ فعالیت
  **صفرند**. گشاد‌کردن قاعده، ۷۱۵ نامعلوم صادق را به ۷۱۵ صفر گمراه‌کننده بدل می‌کرد.
- **برآورد مصالح ۳٬۶۰۷٬۴۳۸٬۹۶۷٬۷۷۷ ریال:** مستقل بازتولید شد. تابع پله‌ای برش تاریخ است؛
  از ۲۰۲۶-۰۹-۰۹ معتبر، پیش از آن در دسترس نیست. **اعتبار جاری دارد، تاریخی ندارد.**
- **گزارش دوره‌ای:** تصمیم عمدی محصول در `36760b45`، نه پس‌رفت.

### ب) آزمایش import فایل امروز — روی دیتابیس جدا

- **هسته می‌پذیرد:** +۱ snapshot، +۳۲۸ فعالیت، +۸۱ منبع، +۷۲۷ تخصیص. اجرای دوم `MPP_DUPLICATE_FILE`.
- **مالی امتناع می‌کند:** `MPP_CURRENCY_UNRESOLVED` — فایل `IRR` می‌گوید و «تومان» نشان می‌دهد،
  و تصمیم تومان به دو sha دقیق گره خورده که این فایل سوم یکی از آن‌ها نیست.
  امتناع **اتمی** بود: هر هفت جدول مالی بدون تغییر.

### ج) AT-23 نیمهٔ PG16 — بسته

روی `bambo_at23_pg16`: `upgrade head` دو بار متوالی، اثرانگشت شِما **یکسان**
(`220d52de287bcb3e0a4b28f8d99284f4`، ۴۵۶ ستون/۱۹۶ قید/۱۱۸ ایندکس/۱۲ trigger)،
سپس `downgrade base` و `upgrade head` دوباره → **همان اثرانگشت اولیه**.
این هم idempotency را اثبات کرد هم مسیر rollback را.

### د) آزمون‌های کیت — ۲۹/۲۹ Pass

`E:\bamboo\kit-run` v1.1.0، `tests/run_contract_tests.py` → `total=29 failures=0 errors=0`.
**ولی این کیت را می‌آزماید، نه ماژول را.** بند «اجرای ماژول مقابل mock host» با تصمیم شما
**نامرتبط** ثبت شد: منبع دادهٔ ماژول دیتابیس است و هیچ adapterی کلاینت HTTP نیست.

### ه) چاپ A4 (AT-24) — بسته

نمودار سطح‌یک در media چاپ ۱۲۸۹px داخل ویوپورت ۶۰۱px بود؛ هشت فاز از سیزده اصلاً چاپ نمی‌شد.
دو قاعدهٔ `@media print`: **۱۱ عنصر بریده → ۴**، و هر چهار باقی‌مانده `sr-only` عمدی‌اند.

### و) دسترسی — ۲۳ + ۶ سناریو

۲۳ سناریو با هویت‌های موجود، سپس ۶ معیار پذیرش با چهار هویت ساختگی.
**تفکیک دوطرفهٔ مجوز اثبات شد:** `finance.edit` شامل `manage_invoice` نیست و برعکس هم نه.
**عدم افشا اثبات شد:** برای هویت بی‌دسترسی، شناسهٔ واقعی و جعلی **هر دو ۴۰۳** — یکسان.

### ز) یک نقص واقعی در دیتابیس کاری (تعمیر نشد)

نسخهٔ فعال `bf456698` در `bambo_canonical_test` اعلام می‌کند `row_count = 789` و **۷۸۸** دارد.
ردیف غایب: `task 1209 / assignment 8847 / بدون منبع` — WBS 1.2، «تحویل زمین»، milestone بدون مبلغ.
**هیچ عدد گزارشی جابه‌جا نمی‌شود.** ولی sync بعدی **شمارنده را به ۷۸۸ می‌آورد، نه ردیف را برمی‌گرداند**.
بازیابی بی‌خطر موجود است: یک `sync` با `refresh=true` روی همان بایت‌ها. **تصمیم شماست.**

---

## ۵. آنچه باز است

### P0 — مانع انتشار (۷ مورد، هیچ‌کدام کار کد نیست)

| # | مورد | مسئول | معیار بسته‌شدن |
|---|---|---|---|
| ۱ | ریز یا درشت بودن مدل مجوزها (PRD §۵.۲ یازده کد پیشنهاد می‌کند، کد دو تا دارد) | مالک محصول | سند امضاشده |
| ۲ | سند نگاشت مجوز، بازبینی و آزمایش‌شده (Kit 04 آن را merge-blocking می‌نامد) | میزبان + ماژول | سند + آزمون منفی |
| ۳ | `finance.manage_invoice` در RBAC واقعی ثبت شود | میزبان | `GET /files/{id}/content` → ۲۰۰ |
| ۴ | قاعدهٔ تأییدکنندهٔ فاکتور: Kit 05 آن را به ثبت‌کننده محدود می‌کند، تصمیم قبلی حذفش کرده | مالک محصول | اصلاح کیت یا بازگرداندن قاعده |
| ۵ | انتخاب Provider واقعی OCR/Voice فارسی | مالک محصول | تأیید کتبی |
| ۶ | تصمیم Malware Scanning (PRD §۱۳.۲ نبودش را «مانع Go-live» نامیده) | میزبان | سند یا پیاده‌سازی |
| ۷ | اجرای migration روی **PostgreSQL 18** | میزبان + ماژول | دو اجرا + اثرانگشت یکسان |

### P1 — الزام اجباری نسخهٔ اول

`B1` ستون scope روی سه جدول · `B2` قید `numeric(18,0)` روی ستون‌های رو‌به‌فایل ·
`B5` CHECK روی `progress_snapshot_refs.snapshot_status` · `B6` حذف CASCADE از `msp_tasks` ·
`B9` پیشوند ایندکس ترکیبی · `D8` رویداد ممیزی برای `classify()` ·
`E5` خوراک `sourceMethod` (تیم Progress) · `G2` مسیرهای `mpp-imports` بیرون `/finance/` ·
`H2` حالت دسترسی روی صفحات باقی‌مانده (نیازمند هویت میزبان) ·
`I1` سقف صفحه‌بندی ۲۰ endpoint · `I3/I4` صدک ۹۵ با ۵۰هزار خط ·
`I8/I9` مشق بازیابی و Staging روی مقصد · `I10` اجرای migration تولید ·
`J3` `AI_USAGE.md` (غایب) · `J4` گزارش مقایسهٔ دو گزینهٔ AI.

**چهارتای اول (`B5`، `B6`، `B9`، `D8`) به‌علاوهٔ `G2` و `I1` بدون هیچ تصمیم محصولی
قابل شروع‌اند و می‌توانند در یک migration و یک PR بسته شوند.**

### P2

`I2` Export سنگین به‌صورت Job — **مشروط**، و شرطش («عبور از محدودیت Sync») در مقیاس فعلی
تحقق نیافته: CSV ۰.۰۹ ثانیه، XLSX ۰.۱۳ ثانیه. تعیین تکلیف نهایی به `I3/I4` گره خورده.
`D10` پاک‌سازی ۹۰روزهٔ Draft — متن PRD «قابل» است نه «باید».

---

## ۶. یک تصمیم باز از آخرین اصلاح

کامیت `a86faaf4` کاری می‌کند که **هر** دارندهٔ `finance.view` — از جمله ویرایشگر — بتواند
«گزارش مالی» را باز کند. این دقیقاً همان چیزی است که تصمیم تأییدشدهٔ `320759ef` نوشته بود،
ولی آزمون قبلی برای ویرایشگر خلافش را ادعا می‌کرد. من به تصمیم تأییدشده وزن دادم چون آن
آزمون **کپیِ خودِ همان پس‌رفت** بود، نه شاهد مستقل.

**اگر نظر محصول این است که ویرایشگر نباید سطح گزارش را ببیند، فقط همین یک قید اضافه شود.**
بقیهٔ اصلاح مستقل از آن است.

---

## ۷. قیدهایی که در کل کار رعایت شد و باید بماند

- ۸۰۴۰ و `bambo_canonical_test` لمس نشدند؛ هیچ ری‌استارت، هیچ نوشتنی.
- هیچ مجوز واقعی تغییر نکرد؛ هویت‌های آزمایشی فقط در کپی یک‌بارمصرف ساخته شدند.
- فایل MPP اصلی تغییر نکرد (sha پیش و پس یکی).
- توسعهٔ Google Sheets از سر گرفته نشد؛ `material_sheet.py` و آزمونش دست‌نخورده ماندند.
- هیچ merge به `master`، هیچ deploy.
- هیچ اعتبارنامه یا فایل منبعی وارد Git نشد.
- `git stash -u` استفاده نشد.

---

## ۸. چطور ادامه دهید

```bash
# کاندیدا
git -C E:\bamboo\FINANCE-api-audit log --oneline -1     # باید a86faaf4 باشد

# سوئیت‌ها (JRE الزامی است، وگرنه ۱۱ خطای کاذب)
cd E:\bamboo\FINANCE-api-audit\backend
set MPP_JAVA_HOME=E:/bamboo/jre/jdk-17.0.20.1+1-jre
python -m pytest -q                                     # 1228 passed

cd ..\frontend && npm test                              # 479 passed
# ممیزی چیدمان به یک سرور استاتیک روی ۴۳۱۲۷ نیاز دارد:
python -m http.server 43127 --bind 127.0.0.1
node scripts/layout-audit.mjs                           # 170 runs, 0 failures
```

**پیش از هر migration روی هر دیتابیسی:**
`python scripts/install_preflight.py --dsn <DSN>` — فقط‌خواندنی، پیش‌نیازهای Core را می‌سنجد.

---

## ۹. آنچه در این کار ساخته شد ولی در مخزن نیست

ابزارهای بررسی در پوشهٔ موقت جلسه ماندند (با پایان جلسه از بین می‌روند).
اگر لازم شدند دوباره ساخته می‌شوند: خوانندهٔ خام مقادیر MPP، آزمایش import و sync،
اثرانگشت شِما، ممیزی چاپ A4، و درایور مرورگر با هویت دلخواه.
**تنها چیزی که ماندنی است، همان است که کامیت شد.**

---

## ۱۰. پاک‌سازی، هر وقت بگویید

سه دیتابیس یک‌بارمصرف (`bambo_apiaudit_trial`، `bambo_at23_pg16`، `bambo_authtest_20260912`)
و پورت ۸۰۴۵ هیچ وابستگی‌ای ندارند و قابل حذف‌اند. ۸۰۴۲ تا وقتی شواهد ممیزی لازم است بماند.
