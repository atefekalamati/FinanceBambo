# ماتریس بازدارنده‌های بیرونی — فاز ۴-ج

**هر چیزی که زیر کنترل ماژول مالی بود، آماده است.** آنچه مانده، کار تیم‌های دیگر است.

**وضعیت مالی:** Alembic `0006` · ۶۱۶ تست بک‌اند (Python 3.12) · ۲۸۲ تست فرانت‌اند ·
اعتبارسنجی‌شده روی PostgreSQL 18.

---

## مسیر بحرانی

### ۱. مجوز `finance_report.issue`

| | |
|---|---|
| **مالک** | تیم **Core** |
| **چه لازم داریم** | یک ردیف در `permissions` با `finance_report` / `issue` / `finance_report.issue`، سپس انتساب نقش |
| **آزمون پذیرش** | ردیف موجود · نقش‌های تأییدشده دارند · بقیه ندارند · سلب فردی غالب · `POST /report-snapshots` فقط برای مجاز ۲۰۰ |
| **وضعیت مالی** | ✅ آماده — فایل‌بسته، تست‌شده. صفر تغییر کد لازم |
| **سند** | `HANDOFF_CORE_PERMISSION_FA.md` |

> کوچک‌ترین کار، طولانی‌ترین انتظار. زودتر مطرحش کنید.

### ۲. هویت Session میزبان

| | |
|---|---|
| **مالک** | تیم **Host/Core** |
| **چه لازم داریم** | یک تابع `identity(request)` که `(user_id, organization_id, project_id)` برمی‌گرداند + سیم‌کشی سه Adapter |
| **آزمون پذیرش** | هویت می‌رسد · `org_chief` درست resolve می‌شود · بین‌سازمانی ۴۰۳ · بین‌پروژه‌ای ۴۰۳ · سلب غالب · هر ۵۷ مسیر محافظت‌شده |
| **وضعیت مالی** | ✅ آماده — `coreint/security.py` نوشته و تست‌شده. فقط درز هویت باز است |
| **سند** | `HANDOFF_HOST_AUTH_FA.md` |

> **بدون این، هیچ مسیری در Production آزمودنی نیست.** بالاترین اولویت.

### ۳. تأمین مقدار اجراشده

| | |
|---|---|
| **مالک** | تیم **Progress** |
| **چه لازم داریم** | خوراکی با `resourceCode` + `unit` + `actualQuantity` در سطح تخصیص |
| **آزمون پذیرش** | Snapshot با bigint · نگاشت فعالیت صریح · نگاشت منبع/واحد صریح · مقدار قابل تأمین · نبودش `null` می‌ماند · بدون Parse در مالی · بدون اجرای ساختگی |
| **وضعیت مالی** | ✅ آماده — Port تعریف‌شده، رفتار «غایب ≠ صفر» تست‌شده |
| **سند** | `HANDOFF_PROGRESS_PROVIDER_FA.md` |

> بدون این، ماژول مالی کار می‌کند ولی هر خط `unavailable` با هشدار است. **وضعیتی صادقانه،
> نه خرابی.**

### ۴. فیلد کد فعالیت MSP

| | |
|---|---|
| **مالک** | تیم **Planning** |
| **چه لازم داریم** | یک پاسخ: کدام فیلد MSP کد فعالیت معتبر است؟ |
| **آزمون پذیرش** | فیلد نام‌برده · پایدار بین Snapshotها · همیشه پر · نمونه مقدار |
| **وضعیت مالی** | ✅ آماده — قابل تنظیم. پیش‌فرض `text1 → outline_number → wbs` یک **حدس مستند** است |
| **سند** | همان سند Progress، بخش پرسش |

> یک پاسخ، یک خط پیکربندی. بدون تغییر API.

---

## ثانویه

### ۵. FileStorage تولیدی

| | |
|---|---|
| **مالک** | تیم **Host/Infrastructure** |
| **چه لازم داریم** | پیاده‌سازی `put` / `get` طبق قرارداد نوشته‌شده |
| **آزمون پذیرش** | هر دو کلید مستأجر در خواندن اعمال می‌شوند · بایت‌ها دست‌نخورده · نبودِ فایل استثنا می‌دهد · Timeout دارد |
| **وضعیت مالی** | ✅ قرارداد نوشته شد. `LocalFileStorage` در Production **قابل دسترسی نیست** — `app/` هرگز `devhost` را import نمی‌کند و یک تست تضمینش می‌کند |
| **سند** | `PRODUCTION_FILE_STORAGE_FA.md` |

---

## خارج از مسیر بحرانی

### OCR و استخراج صوت

| | |
|---|---|
| **مالک** | تیم Host/Infrastructure |
| **وضعیت** | ⚪ **NOT_REQUIRED_FOR_MVP** |
| **رفتار فعلی** | `503` صریح. **هیچ خروجی AI ساختگی تولید نمی‌شود** |
| **اثر** | ۵۵ از ۵۷ مسیر بدون آن کار می‌کنند |
| **سند** | `PRODUCTION_OCR_VOICE_FA.md` |

> **این را با مسیر بحرانی قاطی نکنید.** اگر MVP استخراج خودکار نمی‌خواهد، این کار اصلاً
> لازم نیست.

---

## خلاصه یک‌نگاهی

| # | بازدارنده | مالک | مسیر بحرانی |
|---|---|---|---|
| ۱ | `finance_report.issue` | Core | ✅ |
| ۲ | هویت Session میزبان | Host/Core | ✅ **بالاترین** |
| ۳ | مقدار اجراشده | Progress | ✅ |
| ۴ | فیلد کد فعالیت | Planning | ✅ |
| ۵ | FileStorage | Host/Infra | ثانویه |
| — | OCR / Voice | Host/Infra | ⚪ خارج |

**هیچ‌کدام تغییری در ماژول مالی لازم ندارند.** برای هر پنج مورد، سمت ما نوشته و تست شده
است — فقط منتظر طرف مقابل است.

---

## شکاف‌های اعتبارسنجی باقی‌مانده

| شکاف | چرا باز است |
|---|---|
| Smoke test روی محیط مستقر | محیط مستقری وجود ندارد |
| تمرین پشتیبان روی هدف واقعی | دسترسی نداریم و نباید داشته باشیم |
| Pipeline استقرار | تصمیم زیرساختی است، نه کار مالی |

فهرست آماده در `PRODUCTION_SMOKE_TEST_FA.md` — **اجرا نشده**.
