# ممیزی بخش‌به‌بخش ماژول مالی — ۲۰۲۶-۰۹-۱۵

هر عدد این سند از پایگاه دادهٔ **`bambo_canonical_test`**، از APIهای در حال اجرا، یا از خودِ
فایل MPP خوانده شده است. هیچ ادعایی از گزارش‌های پیشین نقل نشده؛ هرچه اینجا نوشته شده در
همین ممیزی دوباره اندازه‌گیری شده است.

**پایه:** شاخهٔ `harden/material-price-db-ops-scripts`، commit `827ca0a5` — که در همین لحظه
با `origin/master` یکی است. درخت کاری تمیز. Alembic روی دیسک `0025`، در پایگاه داده `0025`.
`frontend` نُه commit عقب است ولی `git cherry` نشان می‌دهد هیچ محتوایی ندارد که روی master
نباشد؛ `backend-finance` بیست‌ودو commit عقب و بدون commit یکتا.

**میزبان محلی ممیزی:** `http://127.0.0.1:8060` روی `bambo_canonical_test`، با Coreِ واقعی
برای عضویت و نقش‌ها. هیچ اتصالی به `bambo_canonical_local` و هیچ اتصالی به Production برقرار
نشد؛ تنها DSN استفاده‌شده `postgresql://postgres@127.0.0.1:5432/bambo_canonical_test` است.

---

## بخش ۱ — زمینهٔ پروژه و میزبان

### سناریو

ماژول مالی باید بداند کدام سازمان، کدام پروژه و کدام نسخهٔ فایل زمان‌بندی را می‌خواند. اگر
نداند، باید **بگوید نمی‌داند** — نه اینکه به دادهٔ نمونه برگردد.

### معماری

میزبان چیزی تحویل نمی‌دهد؛ ماژول خودش می‌خواند.

| گام | منبع | جزئیات |
|---|---|---|
| شناسهٔ پروژه | `?project=` در آدرس، وگرنه `localStorage['bambo:project']` | همان ترتیبی که `ProjectScopeSelector` خودِ داشبورد دارد؛ `?building=` هم پذیرفته می‌شود تا لینک‌های قدیمی نشکنند |
| کاربر | `GET /api/me` | `userId` |
| پروژه و سازمان | `GET /api/projects/{id}` | `organizationId`، نام، کد |
| مجوزها | `GET /api/rbac/my-permissions` | اختیاری؛ نبودش Context را از بین نمی‌برد |

سپس هر درخواست مالی **دوباره** در Core بررسی می‌شود. پرچم `isBamboAdmin` عمداً نادیده گرفته
می‌شود، چون `CoreRbacPermissionAuthorizer` هیچ دور زدنی برای مدیر ندارد؛ احترام گذاشتن به آن
پرچم یعنی کشیدن دکمه‌ای که تنها پاسخ ممکنش ۴۰۳ است.

### انتخاب Snapshot

قانون در یک جا نوشته شده (`shared/progress/project-snapshot.js`) و هر پنج صفحه از همان
می‌خوانند:

1. فقط Snapshot با وضعیت `ready` قابل گزارش است.
2. **منبع فعال پروژه** برنده است (`isActiveSource`) — همان نسخه‌ای که ریز برآورد از آن آمده.
3. وگرنه تازه‌ترین.

این مهم است چون در همین پایگاه داده «تازه‌ترین» و «منبع فعال» **یکی نیستند**:

| فایل | نسخه | تاریخ گزارش | isLatest | isActiveSource | هویت |
|---|---|---|---|---|---|
| AB11_V13.mpp | ۵ | ۲۰۲۶-۱۰-۲۲ | ✅ | ✖ | Core (`hostSnapshotId=40`) |
| terrace.mpp | ۴ | ۲۰۲۶-۰۹-۰۵ | ✖ | ✖ | Finance (`sourceFileVersionId`) |
| terrace.mpp | ۳ | ۲۰۲۶-۰۹-۰۴ | ✖ | ✖ | Core (`45`) |
| B10.mpp | ۲ | ۲۰۲۶-۰۸-۲۲ | ✖ | ✖ | Core (`38`) |
| **test_progress.mpp** | ۱ | ۲۰۲۵-۱۰-۰۲ | ✖ | **✅** | Finance |

اگر قانون «تازه‌ترین» بود، اعداد سرصفحه از فایلی خوانده می‌شد که ریز برآورد اصلاً به آن
وصل نیست.

### تاریخ گزارش

`finance-home-page.js` تاریخ گزارش را **امروزِ تهران** می‌گیرد، نه تاریخ Snapshot. Snapshot
می‌گوید کدام واقعیتِ پیشرفت، و تاریخ می‌گوید کدام اسناد مالی — این دو یک چیز نیستند.

### آزمون منفی (اجرا شد)

| ورودی | پاسخ |
|---|---|
| `terrace` | ۲۰۰ |
| `sample_site_01` (پروژهٔ دیگر) | ۴۰۳ `FINANCE_FORBIDDEN` |
| `does-not-exist` | ۴۰۳ `FINANCE_FORBIDDEN` |
| `terrace'--` | ۴۰۳ `FINANCE_FORBIDDEN` |

هیچ‌کدام دادهٔ پروژهٔ دیگری بیرون ندادند و هیچ‌کدام پیام پایگاه‌داده‌ای فاش نکردند.

نبودِ Context هم به `HostContextError` با جملهٔ فارسی می‌رسد، نه به Mock: ماژول‌های
`adapters/mock/` **فقط** وقتی بارگذاری می‌شوند که صفحه `data-finance-runtime="standalone"`
باشد **و** هیچ Contextی از میزبان نیامده باشد. در مرورگر تأیید شد: `runtime = "host"`،
صفر درخواست به `adapters/mock`، صفر درخواست به Google Sheets، صفر خطای Console.

### یافته‌ها

1. **`context.grossBuiltArea` مرده است.** از میزبان خوانده و Normalize می‌شود و هیچ صفحه‌ای
   آن را نمی‌خواند. زیربنای مالی از `finance_project_settings.gross_built_area` می‌آید که
   مسیر درست است. فیلد مرده خطر ندارد ولی دو منبع برای یک عدد، دعوت به اشتباه است.
2. **میزبان توسعه، مشخصات پروژه را می‌سازد.** `GET /api/projects/terrace` نام «پروژه مسکونی
   نمونه»، کد `BMB-1405-01` و زیربنای ۴۲۵۰ برمی‌گرداند، در حالی که ردیف Core نام `+TERRACE`،
   کد `project-1` و زیربنای **۴۷۴۵** دارد. این مربوط به devhost است نه ماژول مالی، ولی یعنی
   نامی که در راستی‌آزمایی محلی دیده می‌شود نامِ پایگاه داده نیست.
3. **زیربنای مالی یک مقدار آزمایشی است.** `finance_project_settings.gross_built_area = 12000`
   با دلیل ثبت‌شدهٔ «TEST ONLY -- local demo gross area». هر عدد «هر مترمربع» روی این
   پایگاه داده بر پایهٔ همین است.

### درصدها — بخش ۱

| سنجه | پیش | پس |
|---|---|---|
| درک معماری | — | ۱۰۰٪ |
| صحت API | ۱۰۰٪ | ۱۰۰٪ |
| صحت پایگاه داده | ۱۰۰٪ | ۱۰۰٪ |
| صحت UI | ۱۰۰٪ | ۱۰۰٪ |
| پوشش تست | ۱۰۰٪ | ۱۰۰٪ (۱۱ تست Discovery + Context + Runtime) |
| آمادگی تحویل | ۹۵٪ | ۹۵٪ |

پنج درصد باقی‌مانده، فیلد مردهٔ `grossBuiltArea` و مقدار آزمایشی زیربناست — هیچ‌کدام نقص کد
نیستند.

---

## بخش ۲ — ورود MSP/MPP و نسخهٔ فایل مبدأ

### سناریو

سامانه فایل MPP را می‌خواند، یک نسخهٔ مبدأ ثبت می‌کند، وظیفه/منبع/تخصیص را بیرون می‌کشد و
ردیف‌های ریز برآورد را می‌سازد.

### معماری و پیکربندی

`MPP_IMPORT_ROOT` **فقط** از پیکربندی خوانده می‌شود (`devhost/environment.py:333`،
`setting("MPP_IMPORT_ROOT")`). هیچ مسیری در کد ماژول Hardcode نشده است.

یک نسخه = یک محتوای متمایز فایل. بایت‌های تکراری یک hash هزینه دارند و **هیچ چیز نمی‌نویسند**
(`status: "unchanged"`)، با قفل برای رقابت هم‌زمان.

### نسخه‌های مبدأ در پایگاه داده

| sha256 | اندازه | ردیف اعلام‌شده | ردیف ذخیره‌شده | تاریخ گزارش |
|---|---|---|---|---|
| `b86b6373…` | ۱٬۶۳۸٬۴۰۰ | ۷۸۹ | ۷۸۹ | — |
| `9792d658…` | ۱٬۵۱۸٬۵۹۲ | ۷۸۹ | **۷۸۸** | ۲۰۲۵-۱۰-۰۲ |
| `55cfe5dc…` (فعال) | ۱٬۵۱۸٬۵۹۲ | ۷۸۹ | ۷۸۹ | ۲۰۲۵-۱۰-۰۲ |

ردیف غایب از `9792d658` شناسایی شد: تخصیص `8847`، وظیفهٔ `1209` «تحویل زمین»، با هزینهٔ صفر
و بدون منبع. این نسخه **Superseded** است و این شکاف سندِ یک نقص تاریخی است؛ ترمیمش شاهد را
پاک می‌کند. نسخهٔ **فعال** کامل است.

### تصمیم ارز

`finance_mpp_currency_decisions` تنها **یک** ردیف دارد، برای `55cfe5dc…`: فایل می‌گوید
symbol=«تومان» و code=`IRR` که با هم نمی‌خوانند؛ تصمیم `amounts_are = 'toman'` با شاهدِ
«مقایسهٔ میدان‌به‌میدان با فایل تأییدشدهٔ `b86b6373`».

ترتیب `_currency_scale` این است و ترتیب، خودِ نکته است:

1. تصمیم ثبت‌شده برای همین بایت‌ها،
2. دو sha تأییدشده در کد (`b86b6373`, `9792d658`)،
3. فایلی که آشکارا ریال می‌گوید.

هر چیز دیگر `None` برمی‌گرداند و `None` یعنی **هزینه‌ها null می‌مانند** — `_file_rials` با
`MPP_CURRENCY_UNRESOLVED` امتناع می‌کند. ضرب در ده **دقیقاً یک بار** و در همان یک تابع
انجام می‌شود.

### راستی‌آزمایی مستقل در برابر فایل واقعی

فایل `b86b6373` روی دیسک هست و در پایگاه داده هم هست، پس مقایسهٔ میدان‌به‌میدان ممکن بود.
با همان Readerِ MPXJ و همان توابع تبدیل خودِ Importer (`_file_units`, `_file_quantity`,
`_file_rials`) — نه با بازنویسی آن‌ها، چون نسخهٔ دوم فقط با خودش موافق می‌شود:

```
تخصیص‌های فایل      : ۷۲۷        ردیف‌های ذخیره‌شده : ۷۸۹
وظیفه‌های فایل      : ۳۲۸        ردیف بدون تخصیص   : ۶۲   (۷۲۷ + ۶۲ = ۷۸۹)
تخصیصی که فایل می‌گوید و ردیفی ندارد : ۰
ردیفی که تخصیصی می‌گوید و فایل ندارد : ۰
میدان‌های مقایسه‌شده : ۷۲۷ تخصیص  →  ۰ اختلاف
صفرِ ساختگی         : ۰           null شدنِ مقداری که فایل می‌گوید : ۰
هزینهٔ صفر: فایل ۴۳۸، ردیف‌ها ۴۳۸
```

**نتیجه: ردیف‌ها همان چیزی را می‌گویند که فایل می‌گوید.**

> سه «اختلاف» در اجرای نخست، خطای خودِ اسکریپت مقایسه بود نه داده: MPXJ واحدهای تخصیص را
> ×۱۰۰ می‌دهد؛ میدان Material دنبالهٔ گرد کردن double دارد؛ و `cost` به‌صورت **رشتهٔ** دهدهی
> می‌آید، پس مقایسه‌اش با عدد `0` همیشه نادرست است و ۴۳۸ «صفر ساختگی» جعلی ساخت.

### هزینه در سطح وظیفه، نه تخصیص

| جمع | مقدار (ریال) |
|---|---|
| `sum(source_assignment_cost_irr)` — **درست** | ۳٬۶۰۵٬۸۲۳٬۹۶۷٬۷۷۵٫۹۰ |
| `sum(source_cost) × 10` — دام | ۲۷٬۲۵۰٬۰۳۵٬۰۷۰٬۳۳۰ (**۷٫۵۶ برابر**) |
| هزینهٔ ثابت، جمع روی ردیف‌ها — دام | ۳۳۹٬۲۲۰٬۸۸۰٬۲۸۸ (**۶٫۶ برابر**) |
| هزینهٔ ثابت، یک‌بار به ازای هر وظیفه — **درست** | ۵۱٬۴۸۴٬۸۸۰٬۰۶۴ |

جست‌وجوی کل کد Backend: **هیچ** `SUM` روی `source_cost` یا `source_fixed_cost` وجود ندارد.
`coreint/finance_activities.py` روی `source_assignment_cost_irr` جمع می‌زند و
`repositories/resources.py` هزینهٔ هر تخصیص را با محدودسازی به **نسخهٔ فعال** می‌خواند.
دام‌ها واقعی‌اند و کسی در آن‌ها نیفتاده است.

### واحدها

`normalized_unit` روی نسخهٔ فعال:

| مقدار | منبع | اطمینان | تعداد |
|---|---|---|---|
| — | `not_a_unit` | low | ۴۴۲ |
| `m3` | alias | alias | ۱۲۴ |
| `m2` | alias | alias | ۷۴ |
| `kg` | alias | alias | ۷۳ |
| — | (ردیف بدون تخصیص) | — | ۶۲ |
| `m` | alias | alias | ۱۰ |
| `each` | alias | alias | ۲ |
| — | `none` | low | ۲ |

هر مقدار حل‌شده یک **کد رجیستری** است. هیچ واحدی ساخته نشده؛ چیزی که شناخته نشده `not_a_unit`
با اطمینان `low` است، که یک یافته است نه یک حدس.

### یافته‌ها

1. **شکاف ۷۸۸/۷۸۹ گرفته می‌شود و انتظارش را داریم.** `scripts/verify_finance_invariants.py`
   روی همین پایگاه داده اجرا شد: **۲۰ از ۲۱ ناوردایی PASS**، و تنها FAIL دقیقاً همین است —
   «source rows match the count their version states» با یک ردیف:
   `{id: bf456698…, stated: 789, actual: 788}`. این نسخه Superseded است و آن شکاف سندِ یک
   نقص تاریخی است؛ ترمیمش شاهد را پاک می‌کند. نسخهٔ فعال کامل است و همین بررسی تأییدش می‌کند.
   (فرض اولیهٔ من این بود که چنین بررسی‌ای وجود ندارد؛ داشت.)
2. **بایت‌های نسخهٔ فعال روی دیسک نیستند.** `55cfe5dc` (۱٬۵۱۸٬۵۹۲ بایت) در هیچ worktree پیدا
   نشد؛ آنچه هست `b86b6373` و یک فایل ذخیره‌نشدهٔ چهارم (`7c048d49`) است که هرگز Import نشده.
   پس نسخهٔ فعال را نمی‌توان از بایت‌های خودش دوباره خواند یا اثبات کرد — فقط sha و اندازه‌اش
   نگه داشته شده است.
3. **`finance_resources.base_unit` متن خام فایل را نگه می‌دارد** روی ۱۷۰ از ۱۸۹ ردیف. تأثیر
   روی ۸۳۵ ردیف ریز برآورد: ۴۲۶ کد رجیستری (`hour`)، ۲۸۹ واژهٔ فارسی که همان‌طور نمایش داده
   می‌شود، و ۱۲۰ ردیف با مقدار لاتینِ `unit` که «واحد تعریف‌نشده» نشان داده می‌شود. نمایش
   صادقانه است — چیزی جعل نمی‌شود — ولی این واحدها **قابل تبدیل نیستند**.
   در میان واژه‌های فارسی، «مترمرکعب» (۵ ردیف) غلط املایی خودِ فایل برای «مترمکعب» است و
   سامانه درست حدس نمی‌زند.

### درصدها — بخش ۲

| سنجه | پیش | پس |
|---|---|---|
| درک معماری | — | ۱۰۰٪ |
| صحت API | ۱۰۰٪ | ۱۰۰٪ |
| صحت پایگاه داده | ۹۵٪ | ۹۵٪ (شکاف ۷۸۸/۷۸۹ روی نسخهٔ Superseded، مستند) |
| صحت UI | ۹۰٪ | ۹۰٪ (۱۲۰ ردیف «واحد تعریف‌نشده») |
| پوشش تست | ۱۰۰٪ | ۱۰۰٪ (۱۱ تست فایل واقعی روی مفسر canonical) |
| آمادگی تحویل | ۹۰٪ | ۹۰٪ |

---

## بخش ۳ — صفحهٔ اقلام و ریز برآورد (`/finance/financial-items`)

### سناریو

ردیف‌های مالی برآمده از MSP/MPP را نشان می‌دهد و اجازه می‌دهد هر قلم به مصالح قیمت روز وصل
شود. ساختار جدول دست‌نخورده ماند: همان نُه ستون، بدون ستون تازه.

### نگاشت ستون‌به‌ستون

| ستون UI | کلید Frontend | فیلد API | سرویس/تابع Backend | جدول · ستون | محاسبه‌شده؟ | Nullable | اگر نباشد |
|---|---|---|---|---|---|---|---|
| فعالیت و قلم هزینه | `identity` | `activityExternalId` + `activityTitle` + منبع `resourceId` | `PsycopgResourceRepository.list_estimate_lines` | `estimate_lines.activity_external_id` · `finance_resources.title/code` | ذخیره‌شده | عنوان فعالیت بله | نام قلم و کد MPP جای آن را می‌گیرند |
| واحد | `unit` | (از `resources`) `baseUnit` | همان | `finance_resources.base_unit` | ذخیره‌شده | خیر | «بدون واحد»؛ مقدار لاتینِ ناشناخته → «واحد تعریف‌نشده» |
| مقدار برآورد اولیه | `originalQuantity` | `originalQuantity` | `effective_original_quantity()` | `estimate_lines.original_quantity` وگرنه `estimate_line_source_completions.quantity` | **محاسبه‌شده** (COALESCE) | بله | «—» |
| آخرین مقدار برآورد | `revisedQuantity` | `revisedQuantity` | آخرین `estimate_revisions.new_quantity` وگرنه مقدار اولیه | `estimate_revisions` | **محاسبه‌شده** | بله | «—» |
| هزینه MSP فعالیت | `scheduleCost` | `mppCostIrr` | زیرپرس‌وجو روی **نسخهٔ فعال** | `finance_mpp_rows.source_assignment_cost_irr` | ذخیره‌شده | بله | «—» |
| قیمت اولیه | `originalPrice` | `originalUnitPriceIrr` | `effective_original_price()` | `estimate_lines.original_unit_price_irr` وگرنه `…completions.unit_price_irr` | **محاسبه‌شده** (COALESCE) | بله | «—» |
| قیمت روز | `currentPrice` | `dailyItemCostIrr` + وضعیت | `ItemPriceComponentService.table_status` | جمع `finance_item_price_mapping_components` روی `price_observations` زنده | **محاسبه‌شده** | بله | تراشهٔ وضعیت فارسی، بدون عدد |
| منبع | `source` | `sourceSummary` / `productName` وگرنه `source` | همان + `sourceLabel(line)` | `provider_items.external_name` · `price_providers.name` | **محاسبه‌شده** | بله | «فایل برنامه زمانی (MPP)» |
| عملیات | `actions` | — | — | — | — | — | دکمه‌ها با `finance.edit` فعال می‌شوند |

### null در برابر صفر — شمارش واقعی روی ۸۳۵ ردیف

| فیلد | null | صفر |
|---|---|---|
| `originalQuantity` | ۴۲۶ | **۰** |
| `originalUnitPriceIrr` | ۴۲۶ | **۰** |
| `mppCostIrr` | ۱۲۰ | ۴۲۶ (صفری که فایل می‌گوید — در بخش ۲ اثبات شد) |
| `actualCostIrr` | ۸۳۵ | ۰ |
| `mppUnit` | ۵۵۲ | — |

هیچ فیلدی صفر نیست آنجا که باید null باشد.

`originalValueSource` منشأ را می‌گوید و خواننده را در حدس نمی‌گذارد:
`recorded` ۱۲۰، `source_completion` ۲۸۹، `null` ۴۲۶.

### چرا ۴۲۶ ردیف هیچ مبنایی ندارند

این‌ها دقیقاً همان ۴۲۶ ردیفی‌اند که واحدشان `hour` است — نیروی انسانی و ماشین‌آلات. فایل MPP
برای آن‌ها نه مقدار مصالح می‌گوید، نه نرخ، و هزینه‌شان را صفر می‌گوید. سامانه هر سه را همان‌طور
گزارش می‌کند.

### ۱۲۰ ردیفی که در نسخهٔ فعال نیستند

`mppCostIrr` روی ۱۲۰ ردیف null است. علت پیدا شد: این ردیف‌ها `source = 'manual_entry'` دارند،
`source_assignment_uid` ندارند، همگی در ۲۰۲۵-۱۲-۳۱ ساخته شده‌اند و واحد پایهٔ همه `unit` است.
یعنی ردیف‌های **دستی**‌اند، نه برآمده از MPP — پس نداشتن هزینهٔ MSP درست است.

### آیا واحد نامعتبر هنوز قابل ثبت است؟ (آزمون زنده)

```
POST /finance/resources  {"baseUnit":"unit", …}
→ 422 {"code":"UNIT_NOT_FOUND","message":"unit is not registered"}
```

هیچ مسیر زنده‌ای نمی‌تواند واحد خارج از رجیستری بنویسد؛ آن ۱۲۰ ردیف دادهٔ قدیمی‌اند. (هیچ
ردیفی با این آزمون ساخته نشد — پاسخ ۴۲۲ بود.)

### راستی‌آزمایی مرورگر (۱۶۰۰×۱۰۰۰)

- هر نُه ستون در عرض دسکتاپ دیده می‌شوند؛ در عرض باریک ستون‌های `SECONDARY` طبق طراحی جمع
  می‌شوند. (اجرای نخست این ممیزی در عرض پیش‌فرض headless بود و همین باعث شد چهار ستون «غایب»
  به نظر برسند — نقص نبود، عرض بود.)
- «تاریخچه» باز می‌شود و حالت خالی را صادقانه می‌گوید: «برای این ردیف هنوز اصلاحی ثبت نشده…».
- «اصلاح مقدار» باز می‌شود با مقدار اولیهٔ **قفل‌شده** و این جمله: «مقدار برآورد اولیه تغییر
  نمی‌کند؛ فقط یک اصلاح جدید ثبت می‌شود» — همان تضمین Append-only.
- «افزودن مصالح» / «ویرایش مصالح» کار می‌کند (بخش ۶).
- جدول دوم صفحه (اقلام مالی) داخل یک `<details>` بسته است؛ سرستون‌هایش درست‌اند
  (کد/عنوان/نوع/واحد پایه/منبع ورود) و «خالی» به نظر رسیدنشان فقط اثر `innerText` روی محتوای
  بستهٔ `<details>` بود.
- صفر خطای Console، صفر Exception، صفر درخواست Mock، صفر درخواست Google Sheets.

### یافته‌ها

1. **۴۲۶ ردیف (۵۱٪) هیچ مبنای برآوردی ندارند.** نقص کد نیست — فایل چیزی نمی‌گوید — ولی یعنی
   نیمی از ریز برآورد مقدار و قیمت اولیه ندارد و صفحه درست همین را نشان می‌دهد.
2. **`actualCostIrr` روی هر ۸۳۵ ردیف null است**، چون هیچ سطر فاکتوری به هیچ ردیف برآوردی
   اشاره نمی‌کند (بخش ۹).
3. **۱۲۰ ردیف «واحد تعریف‌نشده» نشان می‌دهند** چون واحد پایه‌شان `unit` است. نمایش صادقانه
   است ولی این واحدها قابل تبدیل نیستند.

### درصدها — بخش ۳

| سنجه | پیش | پس |
|---|---|---|
| درک معماری | — | ۱۰۰٪ |
| صحت API | ۱۰۰٪ | ۱۰۰٪ |
| صحت پایگاه داده | ۱۰۰٪ | ۱۰۰٪ |
| صحت UI | ۱۰۰٪ | ۱۰۰٪ |
| پوشش تست | ۱۰۰٪ | ۱۰۰٪ |
| آمادگی تحویل | ۸۵٪ | ۸۵٪ |

پانزده درصد باقی‌مانده کیفیت داده است نه کد: نیمی از ردیف‌ها مبنا ندارند و هیچ هزینهٔ واقعی
ثبت نشده.

---

## بخش ۴ — صفحهٔ قیمت روز مصالح (`/finance/prices`)

### معماری

جدول قیمت روز بازار **پشت یک دکمه** بارگذاری می‌شود: «نمایش قیمت روز بازار». این عمدی است و
صفحه هم می‌گوید چرا: «این قیمت‌ها مشاهده‌های وارد‌شده از برگه مصالح‌اند، نه قیمت رسمی مالی
پروژه.» دو جدول روی یک صفحه‌اند و نباید یکی جای دیگری گرفته شود.

هیچ درخواستی از مرورگر به Google Sheets نمی‌رود و هیچ ماژول Mockی بارگذاری نمی‌شود — در
مرورگر شمرده شد: صفر و صفر.

### ستون‌های هر دسته در برابر متادیتای واقعی برگه

مقایسهٔ ستون‌هایی که API می‌دهد با کلیدهایی که **واقعاً** در `provider_items.metadata` هستند:

| دسته | کلیدهای موجود در متادیتا | ستون‌های spec در API | تطابق |
|---|---|---|---|
| نبشی | طول، وزن، ضخامت، تعداد شاخه | همان ۴ | ✅ |
| آجر | قیمت در هر مترمربع، ابعاد، وزن، کد | همان ۴ | ✅ |
| ناودانی | طول، وزن | همان ۲ | ✅ |
| تیرآهن | وزن - کیلوگرم | همان ۱ | ✅ |
| لوله | — | — | ✅ |
| اتصالات لوله | — | — | ✅ |
| پروفیل | طول، وزن، ضخامت، ابعاد | همان ۴ | ✅ |
| میلگرد | واحد - وزن | همان ۱ | ✅ |

**هر هشت دسته دقیقاً همان چیزی را نشان می‌دهند که برگه‌شان دارد، نه بیشتر و نه کمتر.**

> صورت مسئله برای «تیرآهن» ستون «طول» را هم خواسته بود. برگهٔ تیرآهن طول ندارد — هیچ‌کدام از
> ۸۹ قلم آن چنین کلیدی ندارند. طبق قاعدهٔ خودِ همان صورت مسئله («فیلدی که برگه ندارد را
> به‌عنوان دادهٔ واقعی نشان نده») نبودنش درست است.

### شمارش دسته‌ها در UI در برابر پایگاه داده

| دسته | اقلام | فعال | تراشهٔ UI |
|---|---|---|---|
| نبشی | ۱۲ | ۱۲ | (۱۲) |
| آجر | ۲۱۰ | ۲۱۰ | (۲۱۰) |
| ناودانی | ۸۵ | ۸۵ | (۸۵) |
| تیرآهن | ۸۹ | ۸۹ | (۸۹) |
| لوله | ۹۹۲ | ۹۹۲ | (۹۹۲) |
| **اتصالات لوله** | ۲۵۲ | **۰** | **(۰)** |
| پروفیل | ۳۶ | ۳۶ | (۳۶) |
| میلگرد | ۳۱۱ | ۳۱۱ | (۳۱۱) |

تراشه شمار **فعال** را می‌گوید که برای یک فهرست قیمت عدد درستی است — قلمی که از برگه رفته
دیگر قیمت روز ندارد. ولی یعنی کل دستهٔ «اتصالات لوله» (۲۵۲ قلم) غیرفعال است و کلیک روی آن
چیزی نشان نمی‌دهد.

### نقصی که پیدا و رفع شد

Backend برای هر ستون spec یک پرچم `numeric` می‌فرستد و `cellValue` آن را **نادیده می‌گرفت**:
هر spec با `String(value)` رندر می‌شد. نتیجه در مرورگر:

```
پیش:  ضخامت 5.0   وزن 27.0   طول «6 متر»   تعداد شاخه 80.0   قیمت ۹۲٬۲۸۰
پس :  ضخامت ۵     وزن ۲۷     طول «6 متر»   تعداد شاخه ۸۰     قیمت ۹۲٬۲۸۰
```

و برای آجر، «قیمت در هر مترمربع» از `364500.0` به `۳۶۴٬۵۰۰` رسید — کنار قیمتی که در همان
ردیف `۱۳٬۵۰۰` نوشته شده بود. دنبالهٔ `.0` مصنوع صفحه‌گسترده است نه اندازه‌گیری تا یک‌دهم.

ستونی که Backend آن را `numeric` **نمی‌خواند** دست‌نخورده می‌ماند: «۶ متر» یک جمله است و
«۲۰*۱۰*۷» یک اندازه، و قالب‌بندی هیچ‌کدام خواندنشان به‌عنوان کمیت را توجیه نمی‌کند.

سه تست تازه، و Mutation آزموده شد: با برگرداندن به `String(value)` هر سه می‌افتند.

### درصدها — بخش ۴

| سنجه | پیش | پس |
|---|---|---|
| درک معماری | — | ۱۰۰٪ |
| صحت API | ۱۰۰٪ | ۱۰۰٪ |
| صحت پایگاه داده | ۱۰۰٪ | ۱۰۰٪ |
| صحت UI | ۹۰٪ | **۱۰۰٪** |
| پوشش تست | ۹۰٪ | **۱۰۰٪** (۱۹ تست ستون‌های دسته) |
| آمادگی تحویل | ۸۰٪ | ۸۵٪ |

---

## بخش ۵ — پنجرهٔ اتصال قلم به قیمت روز

### وضعیت

پنجرهٔ تک‌محصولی «اتصال قیمت روز» دیگر وجود ندارد؛ کارش را فرم داخل پنل چندمصالحی انجام
می‌دهد (بخش ۶). هر فیلدی که صورت مسئله خواسته بود موجود است، جز دو مورد که عمدی‌اند یا
شکاف‌اند:

| فیلد خواسته‌شده | وضعیت |
|---|---|
| دستهٔ مصالح | ✅ |
| تأمین‌کننده / منبع | ✅ محدودشده به دسته |
| نوع محصول | ✅ محدودشده به دسته **و** تأمین‌کننده |
| جست‌وجوی محصول | ✅ |
| فهرست نامزدها | ✅ از `provider_items` واقعی، مرتب بر اساس دسته و نام — نه بر اساس قیمت |
| مشخصات دسته‌محور | ✅ از `metadata`، فقط آنچه برگه دارد |
| واحد رسمی انتخابی | ✅ از رجیستری |
| واحد قیمت مبدأ | ✅ در پیش‌نمایش، با برچسب خودش |
| وضعیت تبدیل | ✅ تراشهٔ وضعیت + دلیل فارسی |
| **ورودی ضریب تبدیل** | ⛔ **عمداً نیست** |
| دلیل | ✅ الزامی |
| دکمهٔ ثبت | ✅ |
| **دکمهٔ تاریخچه** | ❌ **شکاف** |

**ضریب تبدیل عمداً در فرم نیست**: عددی که کسی داخل فرمِ یک جزء تایپ می‌کند، اندازه‌گیریِ
تأییدشدهٔ کسی نیست. ضریب باید در `provider_item_unit_factors` با تأییدکننده ثبت شود
(`POST /material-prices/{providerItemId}/factors`). این تصمیم درست است — ولی همان‌طور که
پایین می‌آید، **آن نقطهٔ ثبت هیچ رابط کاربری ندارد**.

### شکاف اصلی این ممیزی: Backendهایی بدون درِ ورودی

از سیزده متد Adapter قیمت مصالح، **فقط دو تا** از یک صفحه فراخوانی می‌شوند:

| متد | فراخوان در UI |
|---|---|
| `listCategories` | ۱ |
| `listCurrentPrices` | ۱ |
| `listPriceHistory` | **۰** |
| `listItemFactors` / `saveItemFactor` | **۰** |
| `listLabels` / `saveLabel` / `listLabelHistory` | **۰** |
| `setUnit` / `listUnitSettings` | **۰** |
| `listImportRuns` / `listInvalidRows` / `listUnresolved` | **۰** |

و `componentHistory` هم هیچ فراخوانی ندارد.

Endpointها در OpenAPI هستند، Adapter نگاشتشان می‌کند، Backend کار می‌کند — و هیچ صفحه‌ای
صدایشان نمی‌زند.

### چرا این مهم‌ترین یافته است

روی پروژهٔ `terrace`:

| جدول پیکربندی | ردیف |
|---|---|
| `material_unit_settings` | **۰** |
| `provider_item_labels` | **۰** |
| `provider_item_unit_factors` | **۰** |

و پوشش واحد مبدأ:

| دسته | اقلام | `normalized_unit` | متن `source_unit` |
|---|---|---|---|
| لوله | ۹۹۲ | ۰ | **۰** |
| میلگرد | ۳۱۱ | ۰ | **۳۱۱** |
| اتصالات لوله | ۲۵۲ | ۰ | ۰ |
| آجر | ۲۱۰ | ۰ | ۰ |
| تیرآهن | ۸۹ | ۰ | ۰ |
| ناودانی | ۸۵ | ۰ | ۰ |
| پروفیل | ۳۶ | ۰ | ۰ |
| نبشی | ۱۲ | ۰ | ۰ |

`normalized_unit` روی **هر ۱٬۹۸۷ قلم** null است. تنها میلگرد واحد دارد و فقط چون برگه‌اش
تصادفاً «کیلو» نوشته که `canonical_unit` می‌شناسدش.

یعنی **۱٬۶۷۶ از ۱٬۹۸۷ قلم (۸۴٫۳٪) قیمت‌گذاری‌شدنی نیستند، و رابط کاربری هیچ راهی برای درست
کردنش ندارد** — چون `setUnit`، `saveLabel` و `saveItemFactor` هیچ فراخوانی ندارند. این
دیگر «تصمیم کسب‌وکاری» نیست؛ یک در است که ساخته نشده.

### نقص دومی که پیدا و رفع شد: دو ماژول، دو پاسخ برای یک سؤال

`material_prices` برای یافتن واحد مبدأ **اول برچسب را می‌پرسد**:
`canonical_unit(label.source_unit) or canonical_unit(row.source_unit)`.

`item_price_components` و `item_price_mappings` برچسب را **اصلاً نمی‌پرسیدند**.

پس برچسبی که کسی ثبت می‌کند تا یک قلم قابل استفاده شود، صفحهٔ قیمت‌ها را عوض می‌کرد و ردیف
اقلام مالی همچنان می‌گفت «واحد قیمت مبدأ مشخص نیست» — یک قلم، دو پاسخ، روی دو صفحه از یک
محصول. همان «واژگان دوم» که این بخش از کد مدام دربارهٔ آن هشدار می‌دهد.

رفع: قاعده به یک تابع مشترک منتقل شد — `stated_source_unit(observation, label)` در
`services/material_price_resolution.py` — و هر سه مسیر از همان می‌خوانند. هر دو Repository
حالا برچسب زنده را کنار مشاهده می‌آورند.

دو تست تازه، Mutation آزموده. روی `terrace` که برچسبی ندارد، هیچ عددی تغییر نکرد — پیش و پس
از تغییر همان سه ردیف با همان ارقام (`549358643200`، `9355000000`، `913600`).

### درصدها — بخش ۵

| سنجه | پیش | پس |
|---|---|---|
| درک معماری | — | ۱۰۰٪ |
| صحت API | ۱۰۰٪ | ۱۰۰٪ |
| صحت پایگاه داده | ۱۰۰٪ | ۱۰۰٪ |
| صحت UI | ۵۵٪ | ۶۰٪ |
| پوشش تست | ۹۵٪ | ۱۰۰٪ |
| آمادگی تحویل | ۵۰٪ | ۵۵٪ |

نمرهٔ UI پایین است چون سه رابط لازم (واحد دسته، برچسب محصول، ضریب تبدیل) و دکمهٔ تاریخچهٔ
اتصال ساخته نشده‌اند — نه چون آنچه ساخته شده کار نمی‌کند.

---

## بخش ۶ — چند مصالح برای یک قلم MSP

### وضعیت: زیرساخت **موجود است**

جدول `finance_item_price_mapping_components` (مهاجرت `0025`) دقیقاً همان چیزی است که صورت
مسئله خواسته بود، با یک تفاوت نام: `component_quantity` در آن **محاسبه** می‌شود از
`usage_quantity` و `usage_mode`، نه اینکه مستقیم ذخیره شود — چون «۵۰۰» بدون گفتن اینکه به
ازای هر واحد است یا در کل، عددِ بی‌معناست.

| فیلد خواسته‌شده | در جدول |
|---|---|
| id، organization_id، project_id، estimate_line_id | ✅ |
| finance_resource_id (nullable) | ✅ |
| provider_item_id، selected_unit | ✅ |
| component_quantity | ✅ `component_quantity_decimal` (محاسبه‌شده از mode+usage) |
| source_price_unit، source_price_basis | ✅ |
| conversion_status، conversion_factor_id | ✅ |
| converted_unit_price_irr، component_daily_cost_irr | ✅ |
| effective_from، superseded_at، superseded_by | ✅ |
| created_by، created_at، reason، is_active | ✅ (`active`) |
| **component_label** | ✖ — نام محصول از خودِ `provider_items` خوانده می‌شود |
| **component_id** | ➕ اضافه شد: هویت پایدار یک مصالح در تمام ویرایش‌هایش |

### رفتار، راستی‌آزمایی‌شده در مرورگر روی `bambo_canonical_test`

ردیف «کانال کنی» (فعالیت ۱.۵.۲.۱، ۷٬۵۱۶٫۴ مترمکعب)، دو مصالح با همان کنترل‌های کاربر:

| مصالح | مصرف | مقدار | قیمت روز | هزینه |
|---|---|---|---|---|
| میلگرد آجدار ۱۰ A2 ابهر | ۸۰ کیلوگرم **به ازای هر مترمکعب** | ۶۰۱٬۳۱۲ کیلوگرم | ۹۱۳٬۶۰۰ ریال | **۵۴۹٬۳۵۸٬۶۴۳٬۲۰۰ ریال** |
| لوله استنلس استیل | ۱٬۲۰۰ متر **در کل** | — | — | **—** (واحد قیمت مبدأ مشخص نیست) |
| **جمع ردیف** | | | | **۵۴۹٬۳۵۸٬۶۴۳٬۲۰۰** + «۱ از ۲ قلم مصالح» |

- پس از Reload کامل باقی ماند.
- ستون «منبع» گفت «چند مصالح (۲ قلم)».
- ستون «هزینه MSP فعالیت» دست‌نخورده ماند.
- جزء حل‌نشده «—» ماند، نه صفر.
- صفر خطای Console، صفر Exception.

### درصدها — بخش ۶

| سنجه | پیش (آغاز این مأموریت) | پس |
|---|---|---|
| زیرساخت | ۱۰۰٪ | ۱۰۰٪ |
| صحت API | ۱۰۰٪ | ۱۰۰٪ |
| صحت UI | ۱۰۰٪ | ۱۰۰٪ |
| پوشش تست | ۱۰۰٪ | ۱۰۰٪ (۲۸ + ۴۸ + ۲۷ تست) |
| آمادگی تحویل | ۹۰٪ | ۹۰٪ (دکمهٔ تاریخچهٔ اجزا هنوز در UI نیست) |

---

## بخش ۷ — رجیستری واحد و تبدیل‌ها

### راستی‌آزمایی در برابر مثال‌های خودِ صورت مسئله

هر سه مثال، با اجرای واقعی دامنه:

| مثال | انتظار | نتیجه |
|---|---|---|
| ۱٬۰۰۰ تومان/کیلوگرم → تومان/گرم | ۱ | **۱٫۰۰۰۰۰۰۰۰** ✅ |
| ۹۲٬۵۶۰ تومان/شاخه با ۲۲ کیلوگرم بر شاخه | ۴٬۲۰۷٫۲۷ | **۴۲۰۷٫۲۷** ✅ |
| ۱۷٬۸۱۲٬۵۶۰ تومان/شاخه با ۲۴۰ کیلوگرم بر شاخه | ۷۴٬۲۱۹ | **۷۴۲۱۹** ✅ |

### تبدیل خودکار

| تبدیل | خودکار؟ | ضریب مقدار |
|---|---|---|
| g→kg | ✅ | ۰٫۰۰۱ |
| kg→ton | ✅ | ۰٫۰۰۱ |
| g→ton | ✅ | ۰٫۰۰۰۰۰۱ |
| mm→cm | ✅ | ۰٫۱ |
| cm→m | ✅ | ۰٫۰۱ |
| cm2→m2 | ✅ | ۰٫۰۰۰۱ |
| liter→m3 | ✅ | ۰٫۰۰۱ |

### تبدیلی که باید ضریب محصول بخواهد

`branch→kg`، `branch→m`، `each→m2`، `bag→kg`، `m→kg`، `m3→ton`، `hour→kg` — **هیچ‌کدام
خودکار نیستند**. هیچ ضریبی حدس زده نمی‌شود.

### قاعدهٔ وارون

`quantity_factor(ton, kg) = 1000` و `convert_unit_price(1000 ریال/تن → ریال/کیلوگرم) = 1`.
مقدار ضرب می‌شود، قیمت تقسیم. دقیقاً همان‌طور که باید.

رجیستری ۱۵ واحد دارد و تنها واژگان سامانه است.

### درصدها — بخش ۷

| سنجه | پیش | پس |
|---|---|---|
| درک معماری | — | ۱۰۰٪ |
| صحت محاسبه | ۱۰۰٪ | ۱۰۰٪ |
| صحت UI | ۱۰۰٪ | ۱۰۰٪ |
| آمادگی تحویل | ۶۰٪ | ۶۰٪ |

محاسبه بی‌نقص است؛ ۴۰٪ کسری از نبودِ رابط ثبت ضریب است (بخش ۵) — دامنه می‌تواند تبدیل کند
و هیچ‌کس نمی‌تواند ضریب را وارد کند.

---

## بخش ۸ — گزارش‌های سطح‌بندی WBS

این بخش دو نقص واقعی داشت و هر دو رفع شدند.

### نقص یکم — تاریخ گزارشی که هیچ چیز در آن وجود نداشت

`level-one-page.js` گزارش را با **تاریخ خودِ Snapshot** می‌خواند و `finance-home-page.js`
با **امروز**. همان پروژه، همان نسخهٔ مبدأ، دو پاسخ:

| تاریخ گزارش | جمع برآورد اولیه | مراحلی که رقمی می‌گویند |
|---|---|---|
| ۲۰۲۵-۱۰-۰۲ (تاریخ Snapshot) | **null** | ۰ از ۱۳ |
| ۲۰۲۶-۰۹-۱۵ (امروز) | ۳٬۶۰۷٬۴۳۸٬۹۶۷٬۷۷۷ | ۸ از ۱۳ |

Snapshot می‌گوید کدام واقعیتِ **پیشرفت**؛ تاریخ گزارش می‌گوید کدام سند **مالی**. خواندن در
تاریخ Snapshot یعنی پرسیدن «برآورد در ۱۰ مهر ۱۴۰۴ چقدر بود؟» — پیش از آنکه هیچ‌کدام از این
ردیف‌ها وجود داشته باشند — و پاسخ صادقانه به آن سؤال «هیچ» است. پس صفحه‌ای که با کلیک روی
۳٬۶۰۷٬۴۳۸٬۹۶۷٬۷۷۷ ریال باز می‌شد، در هر ردیف «—» نشان می‌داد.

رفع: `level-one-page.js` حالا `getTehranTodayIso()` می‌خواند، مثل صفحهٔ بالادستش، و
`progressSnapshotId` را دست‌نخورده نگه می‌دارد. خط منشأ هم هر دو تاریخ را جدا می‌گوید:
«تاریخ گزارش: ۲۴ شهریور ۱۴۰۵ · وضعیت برنامه زمانی: ۱۰ مهر ۱۴۰۴».

### نقص دوم — نسخه‌ای که تاریخش را نگفته بود

`missingPriceCount` برابر ۷۱۵ بود: ۴۲۶ ردیف بی‌مبنا **به‌علاوهٔ ۲۸۹ ردیفی که در صفحهٔ اقلام
قیمت اولیه نشان می‌دادند**. یعنی همان ۲۸۹ ردیف روی یک صفحه رقم داشتند و روی صفحهٔ دیگر
«وجود ندارد» بودند.

ریشه: هر ۲۸۹ `estimate_line_source_completions` به نسخهٔ `b86b6373` اشاره می‌کنند و
`reporting_date` آن نسخه **NULL** بود. قاعدهٔ `_source_effective_cutoff` می‌خواهد
`reporting_date IS NOT NULL AND <= cutoff` — پس **صفر** تکمیل از سد می‌گذشت. صفحهٔ اقلام
(بدون cutoff) نشانشان می‌داد؛ گزارش (با cutoff) کنارشان می‌گذاشت.

چرا NULL بود؟ آن نسخه پیش از مهاجرت `0014` وارد شده بود، وقتی Reader هنوز `Status Date` را
بیرون نمی‌کشید. **خودِ فایل روی دیسک `Status Date = 2025-10-02` می‌گوید** — همان تاریخی که
دو نسخهٔ دیگر دارند.

رفع: با **مسیر پشتیبانی‌شدهٔ خودِ محصول**، نه با UPDATE دستی. Importer وقتی بایت‌ها تغییر
نکرده باشند و `reporting_date` خالی باشد، آن را از `status_date` فایل پر می‌کند و هیچ چیز
دیگری نمی‌نویسد. پیش از اجرا Backup گرفته شد
(`bambo_canonical_test_20260915_031727.dump`، sha `e316657f…`، ۶۷۴ entry، validated).

```
POST /finance/mpp-sync {"fileName":"test_progress.mpp"}
→ 201 {"status":"unchanged", "rowCount":789}
```

«unchanged» یعنی هیچ ردیفی بازنویسی نشد. نتیجه:

| | پیش | پس |
|---|---|---|
| `reporting_date` نسخهٔ `b86b6373` | NULL | ۲۰۲۵-۱۰-۰۲ |
| تکمیل‌هایی که از cutoff می‌گذرند | ۰ | **۲۸۹** |
| `overview.initialEstimateIrr` | ۱٬۶۱۵٬۰۰۰٬۰۰۰ | **۳٬۶۰۷٬۴۳۸٬۹۶۷٬۷۷۷** |

و این عدد با فایل می‌خواند:
۳٬۶۰۵٬۸۲۳٬۹۶۷٬۷۷۵٫۹۰ (جمع MPP) + ۱٬۶۱۵٬۰۰۰٬۰۰۰ (۱۲۰ ردیف دستی) = ۳٬۶۰۷٬۴۳۸٬۹۶۷٬۷۷۵٫۹۰ —
اختلاف ۱٫۱ ریال از گرد کردن هر ردیف.

### سطح ۲ درست کار می‌کند و جمع می‌خورد

| والد | فرزندان | جمع فرزندان | رقم سطح ۱ |
|---|---|---|---|
| ۱.۵ | ۳ | ۶۹۲٬۵۵۲٬۳۰۷٬۸۹۷ | ۶۹۲٬۵۵۲٬۳۰۷٬۸۹۷ ✅ |
| ۱.۴ | ۲ | ۱۵۷٬۰۵۶٬۱۵۳٬۹۱۰ | ۱۵۷٬۰۵۶٬۱۵۳٬۹۱۰ ✅ |

### مرحله‌ای که زیرمجموعه ندارد — نقص نبود

فرضیه‌ام این بود که کلیک روی «۱.۱ شروع» صفحهٔ خالی و بی‌توضیح می‌دهد. **غلط بود.** صفحه
رقم خودِ مرحله (۱۰۰ میلیون تومان)، هزینهٔ واقعی، ترکیب هزینه، و این جمله را نشان می‌دهد:
«برای این مرحله زیرمجموعه‌ای در ساختار پروژه ثبت نشده است.» سنجهٔ اول من فقط
`.empty-state, .state-card` را می‌خواند و این اعلان کلاس `inline-notice` دارد.

### یافتهٔ کیفیت داده

آن ۱۰۰ میلیون تومانِ مرحلهٔ «۱.۱ شروع» از یک ردیف دستی می‌آید به نام
«آخرين تاريخ بروزرساني» با مقدار ۱۰۰۰ و قیمت ۱٬۰۰۰٬۰۰۰ — تقریباً قطعاً دادهٔ آزمایشی است،
نه یک هزینهٔ واقعی.

### ناوردایی‌ها پس از ترمیم

دوباره اجرا شد: **۲۰ از ۲۱ PASS**، و همان یک FAIL شناخته‌شده (شکاف ۷۸۸/۷۸۹ روی نسخهٔ
Superseded). ترمیم چیزی را خراب نکرد.

### درصدها — بخش ۸

| سنجه | پیش | پس |
|---|---|---|
| درک معماری | — | ۱۰۰٪ |
| صحت API | ۹۵٪ | ۱۰۰٪ |
| صحت پایگاه داده | ۷۰٪ | **۱۰۰٪** |
| صحت UI | ۲۰٪ | **۹۵٪** |
| پوشش تست | ۷۰٪ | ۹۰٪ (۴ تست تازهٔ تاریخ گزارش) |
| آمادگی تحویل | ۲۵٪ | **۸۵٪** |

---

## بخش ۹ — فاکتورها و هزینهٔ واقعی

### وضعیت داده

| | |
|---|---|
| فاکتورها | **۲**، هر دو `draft`، `financial_effect_sign = 1`، جمع ۱۵٬۵۹۰٬۲۳۰٬۰۰۰ ریال |
| سطرهای فاکتور | ۲، **هر دو** به یک ردیف برآورد اشاره می‌کنند |
| تریگر | `confirmed_invoice_immutable` روی `invoices` |

### دروازهٔ وضعیت — آزموده شد

`actualCostIrr` روی **هر ۸۳۵ ردیف برآورد** null است. علتش درست است: پرس‌وجو
`i.status IN ('confirmed','voided','corrected')` می‌خواهد و هر دو فاکتور `draft` هستند.
**یک فاکتور پیش‌نویس هزینهٔ واقعی نیست** و سامانه آن را نمی‌شمارد.

در گزارش WBS اما `actualCostIrr = 0` است، نه null — و این تفاوت عمدی و درست است: ما
**می‌دانیم** هیچ فاکتور تأییدشده‌ای وجود ندارد، پس هزینهٔ واقعی صفر است. این با برآوردی که
مبنا ندارد فرق دارد؛ آن یکی نامعلوم است و null می‌ماند.

### دسترسی به فایل پیوست

بایت‌های فایل پشت `finance.manage_invoice` است و **۴۰۴ برمی‌گرداند نه ۴۰۳** — عمداً: «اجازه
نداری این فایل را بخوانی» خودش می‌گوید فایل وجود دارد، و روی شناسه‌ای که کسی حدس زده، همین
تفاوت دقیقاً همان چیزی است که دنبالش بوده. متادیتای فایل روی `finance.view` می‌ماند.

### درصدها — بخش ۹

| سنجه | پیش | پس |
|---|---|---|
| درک معماری | — | ۱۰۰٪ |
| صحت API | ۱۰۰٪ | ۱۰۰٪ |
| صحت پایگاه داده | ۱۰۰٪ | ۱۰۰٪ |
| صحت UI | ۱۰۰٪ | ۱۰۰٪ |
| آمادگی تحویل | ۶۰٪ | ۶۰٪ |

چهل درصد کسری دادهٔ واقعی است، نه کد: هیچ فاکتور تأییدشده‌ای وجود ندارد، پس کل مسیر هزینهٔ
واقعی روی دادهٔ واقعی آزموده نشده است.

---

## بخش ۱۰ — تاریخچهٔ قیمت و رد ممیزی

### چه چیز Append-only است و چه چیز تغییرپذیر

| جدول | ردیف | نسخه‌بندی |
|---|---|---|
| `provider_item_labels` | ۳ | `version`, `superseded_at`, `superseded_by` |
| `provider_item_unit_factors` | ۱ | `version`, `superseded_at`, `superseded_by` |
| `finance_item_price_mappings` | ۵ | `version`, `superseded_at`, `superseded_by` |
| `finance_item_price_mapping_components` | ۱۶ | `version`, `superseded_at`, `superseded_by` |
| `material_unit_settings` | ۰ | `version`, `superseded_at`, `superseded_by` |
| `estimate_revisions` | ۰ | `revision` |
| `finance_project_settings` | ۱ | `revision` |
| `price_versions` | ۱۲۳ | `version` |
| `price_observations` | ۳٬۸۲۱ | — (واقعیت تغییرناپذیر: مشاهدهٔ تازه = ردیف تازه) |
| `price_collection_runs` | ۲ | — (همان) |
| `finance_audit_events` | ۵۱ | — (فقط افزودنی) |
| `report_snapshots` | ۱۶ | — (هر کدام Payload خودش را منجمد کرده) |

این شکل درست است. جایی که **معنای مالی** عوض می‌شود نسخه‌بندی هست؛ جایی که یک واقعیت ثبت
می‌شود (یک مشاهده، یک رویداد، یک گزارش صادرشده) ردیف تازه است و قدیمی دست‌نخورده می‌ماند.

### رویدادهای ممیزی ثبت‌شده

| رویداد | تعداد |
|---|---|
| `attachment.accessed` | ۳۱ |
| `report_snapshot.issued` | ۱۶ |
| `invoice.created` | ۲ |
| `finance_import.committed` | ۱ |
| `finance_resource.created` | ۱ |

### شکاف

همان شکاف بخش ۵: `listPriceHistory`، `listLabelHistory`، `listItemFactors`،
`listImportRuns`، `listInvalidRows`، `listUnresolved` و `componentHistory` **هیچ فراخوانی
در UI ندارند**. تاریخچه ثبت می‌شود و کسی نمی‌تواند ببیندش.

### درصدها — بخش ۱۰

| سنجه | پیش | پس |
|---|---|---|
| صحت پایگاه داده | ۱۰۰٪ | ۱۰۰٪ |
| صحت API | ۱۰۰٪ | ۱۰۰٪ |
| صحت UI | ۳۰٪ | ۳۰٪ |
| آمادگی تحویل | ۵۵٪ | ۵۵٪ |

---

## بخش ۱۱ — مجوزها و نقش‌ها

### ماتریس مجوز، از خودِ Router

| مجوز | تعداد Endpoint |
|---|---|
| `finance.view` | ۴۲ |
| `finance.edit` | ۱۹ |
| `finance.manage_invoice` | ۱۱ |
| `finance_report.view` | ۶ |
| `finance_report.export` | ۲ |
| `finance_report.issue` | ۱ |

از ۸۹ Endpoint، ۸۴ مستقیماً `_resource_scope(...)` را با یک مجوز صدا می‌زنند. پنج تای دیگر
(`imports/estimate/preview`، `preview-link`، `commit`، `imports/prices/commit`،
`files/{fileId}/content`) به کمکی‌هایی واگذار می‌کنند که خودشان مجوز می‌خواهند — بررسی شد:
`_preview_import` با `finance.edit` شروع می‌شود.

**هیچ Endpointی بدون بررسی مجوز نیست.**

### اجرا سمت سرور است، نه فقط UI

با `curl` و بدون هیچ Frontendی، سه پروژه‌ای که این کاربر عضوشان نیست ۴۰۳ گرفتند. Frontend هم
پرچم `isBamboAdmin` را عمداً نادیده می‌گیرد، چون `CoreRbacPermissionAuthorizer` هیچ دور زدنی
برای مدیر ندارد و احترام به آن پرچم یعنی کشیدن دکمه‌ای که تنها پاسخش ۴۰۳ است.

### آنچه آزموده **نشد** — و چرا

این پایگاه داده فقط **یک** هویت با مجوز مالی دارد (`admin@bambo.local` با ۵ کد). نقش دوم
«رییس سازمان» هیچ کاربری ندارد. پس **آزمون زندهٔ یک کاربر کم‌دسترسی انجام نشد** و ساختن
کاربر برای آن، جعل داده بود. این را به‌عنوان پوشش ادعا نمی‌کنم.

### درصدها — بخش ۱۱

| سنجه | پیش | پس |
|---|---|---|
| صحت API | ۱۰۰٪ | ۱۰۰٪ |
| اجرای سمت سرور | ۱۰۰٪ | ۱۰۰٪ |
| پوشش تست زنده | ۵۰٪ | ۵۰٪ (کاربر کم‌دسترسی وجود ندارد) |
| آمادگی تحویل | ۸۵٪ | ۸۵٪ |

---

## بخش ۱۲ — بدون دادهٔ ساختگی

### بستهٔ تحویلی — ساخته و شمرده شد

```
node scripts/build-package.mjs  ->  125 files (1 html, 99 js, 23 css, 2 asset)
فایلی با نام mock / demo / fixture : هیچ
index.html                        : نیست (تنها صفحه‌ای که پیش‌نمایش را روشن می‌کند)
host.html                         : هست (همانی که میزبان mount می‌کند)
```

بسته **حاوی دادهٔ نمونه نیست**، نه اینکه صرفاً اجرایش نکند.

### در زمان اجرا — در مرورگر شمرده شد

روی هر صفحه‌ای که در این ممیزی باز شد (`operations`، `financial-items`، `prices`،
`level-one`):

| | |
|---|---|
| `document.body.dataset.financeRuntime` | `host` |
| درخواست به `adapters/mock` | **۰** |
| درخواست به Google Sheets | **۰** |
| خطای Console | **۰** |
| Exception مدیریت‌نشده | **۰** |

همهٔ درخواست‌های داده به `/api/projects/terrace/finance/...` رفتند.

### Backend

- بستهٔ `app/finance` هیچ ماژول seed یا demo وارد نمی‌کند.
- `seeding_allowed()` **Fail-closed** است: دو شرط مثبت صریح، هیچ‌کدام با پیش‌فرض. APP_ENV
  تنظیم‌نشده یعنی رد، نه «development».
- در این ممیزی هیچ Seed اجرا نشد و `FINANCE_ALLOW_SEED` تنظیم نشد.

### دادهٔ آزمایشیِ موجود در پایگاه داده (نه Mock در زمان اجرا، ولی گفتنی)

1. `finance_project_settings.gross_built_area = 12000` با دلیل «TEST ONLY -- local demo
   gross area» — پایهٔ هر عدد «هر مترمربع».
2. ردیف دستی «آخرين تاريخ بروزرساني» با ۱۰۰۰ × ۱٬۰۰۰٬۰۰۰ = ۱ میلیارد ریال که تمام برآورد
   مرحلهٔ «۱.۱ شروع» است.
3. ۱۲۰ ردیف `manual_entry` با واحد پایهٔ `unit` که «واحد تعریف‌نشده» نشان داده می‌شوند.

اینها دادهٔ پایگاه دادهٔ آزمون‌اند و در Production نخواهند بود، ولی هر گزارشی که روی همین
پایگاه داده گرفته شود شاملشان است.

### درصدها — بخش ۱۲

| سنجه | پیش | پس |
|---|---|---|
| خلوص بستهٔ تحویلی | ۱۰۰٪ | ۱۰۰٪ |
| خلوص زمان اجرا | ۱۰۰٪ | ۱۰۰٪ |
| محافظ Seed | ۱۰۰٪ | ۱۰۰٪ |
| کیفیت دادهٔ پایگاه آزمون | ۷۵٪ | ۷۵٪ |
| آمادگی تحویل | ۹۵٪ | ۹۵٪ |

---

# جمع‌بندی تحویل

## نقص‌هایی که پیدا و رفع شدند

| # | بخش | نقص | لایهٔ رفع |
|---|---|---|---|
| ۱ | ۴ | ستون spec عددی خام رندر می‌شد (`364500.0` کنار `۱۳٬۵۰۰`) | Presenter فرانت‌اند |
| ۲ | ۵ | برچسب محصول واحد مبدأ را تعیین می‌کرد، ولی فقط برای صفحهٔ قیمت‌ها؛ مسیر قیمت‌گذاری قلم برچسب را نمی‌پرسید | سرویس Backend (یک قاعدهٔ مشترک) |
| ۳ | ۸ | صفحهٔ گزارش مراحل با تاریخ Snapshot خوانده می‌شد و صفحهٔ بالادستش با امروز | صفحهٔ فرانت‌اند |
| ۴ | ۸ | `reporting_date` نسخهٔ مبدأ NULL بود، پس ۲۸۹ ردیف از گزارش بیرون می‌ماندند | دادهٔ پایگاه، از مسیر پشتیبانی‌شدهٔ خودِ محصول |

## نقص‌هایی که پیدا شدند و رفع **نشدند**

| # | بخش | مسئله | چرا رفع نشد |
|---|---|---|---|
| ۵ | ۵، ۷، ۱۰ | یازده متد Adapter بدون هیچ فراخوانی در UI: تاریخچهٔ قیمت، ضرایب تبدیل، برچسب محصول، تنظیمات واحد، اجراهای Import، ردیف‌های نامعتبر، اقلام حل‌نشده | سه رابط کاربری تازه لازم دارد؛ اندازه‌اش از یک ممیزی بزرگ‌تر است و باید جداگانه تصمیم‌گیری شود |
| ۶ | ۵ | دکمهٔ تاریخچهٔ اتصال در پنل مصالح نیست | همان |
| ۷ | ۲ | بایت‌های نسخهٔ فعال (`55cfe5dc`) روی دیسک نیست | تصمیم نگهداری فایل، نه نقص کد |
| ۸ | ۲ | `JavaRuntimeNotAvailable` به‌صورت ۵۰۰ خام بیرون می‌آید | کوچک است ولی خارج از دامنهٔ رفع‌های این دور |

## فرضیه‌هایی که غلط بودند و ثبت شده‌اند

- گمان کردم بررسی «row_count در برابر ردیف‌های ذخیره‌شده» وجود ندارد. **وجود دارد** و همان
  یک شکاف شناخته‌شده را گزارش می‌کند.
- گمان کردم مرحلهٔ بدون زیرمجموعه صفحهٔ خالی و بی‌توضیح می‌دهد. **نمی‌دهد**.
- سه «اختلاف» در مقایسهٔ فایل MPP، خطای اسکریپت خودم بود نه داده.
- «ستون‌های غایب» جدول ریز برآورد، اثر عرض پیش‌فرض headless بود نه نقص.
- «سرستون‌های خالی» جدول دوم، اثر `innerText` روی محتوای `<details>` بسته بود.

## آزمون‌ها

| | |
|---|---|
| Backend | **۱٬۶۸۰ موفق** (۱۱ تست فایل واقعی MPP جداگانه روی مفسر canonical، موفق) |
| Frontend | **۶۴۷ موفق** |
| قرارداد OpenAPI | `--check` تمیز، ۷۳ مسیر |
| ناوردایی‌های مالی | ۲۰ از ۲۱ PASS؛ تنها FAIL، شکاف مستندشدهٔ ۷۸۸/۷۸۹ روی نسخهٔ Superseded |
| Mutation | چهار رفع، هر کدام با برگرداندن تغییر آزموده شد و تست‌ها افتادند |

## شواهد مرورگر

Chrome واقعی، ۱۶۰۰×۱۰۰۰، روی `bambo_canonical_test`:
`operations`، `financial-items`، `prices` (هر هشت دسته)، `level-one`، `level-one?wbs=1.5`،
`level-one?wbs=1.1`. صفر خطای Console، صفر Exception، صفر درخواست Mock، صفر درخواست Sheets.

## ایمنی پایگاه داده

- تنها DSN استفاده‌شده: `postgresql://postgres@127.0.0.1:5432/bambo_canonical_test`.
- هیچ اتصالی به `bambo_canonical_local`، هیچ اتصالی به Production.
- پیش از تنها نوشتن ماندگار، Backup گرفته و اعتبارسنجی شد:
  `bambo_canonical_test_20260915_031727.dump`، sha `e316657f394ce804…`، ۶۷۴ entry.
- هیچ `DROP`، `TRUNCATE`، `DELETE`، restore، یا غیرفعال‌سازی تریگر اجرا نشد.
- تنها نوشتن: یک `mpp-sync` که `{"status":"unchanged"}` برگرداند و یک ستون تاریخ را از
  خودِ فایل پر کرد.

## بلوکرهای باز

1. **۱٬۶۷۶ از ۱٬۹۸۷ قلم (۸۴٫۳٪) قیمت‌گذاری‌شدنی نیستند** و رابط کاربری راهی برای درستش
   ندارد. تنها میلگرد واحد مبدأ دارد. این بلوکر شمارهٔ یک تحویل است.
2. **هیچ فاکتور تأییدشده‌ای وجود ندارد**، پس مسیر هزینهٔ واقعی روی دادهٔ واقعی آزموده نشده.
3. **۴۲۶ از ۸۳۵ ردیف (۵۱٪) هیچ مبنای برآوردی ندارند** — فایل برایشان چیزی نمی‌گوید.
4. **دادهٔ آزمایشی در پایگاه آزمون** (زیربنای ۱۲۰۰۰، ردیف ۱ میلیارد ریالی «آخرين تاريخ
   بروزرساني») در هر گزارشی از همین پایگاه دیده می‌شود.

## درصد آمادگی

| | درصد |
|---|---|
| **آماده برای آزمون داخلی** | **۸۸٪** |
| **آماده برای مهاجرت Production** | **۷۵٪** |
| **آماده برای تحویل نهایی** | **۶۰٪** |

### چرا تحویل نهایی ۶۰٪ است

نه به این دلیل که چیزی که ساخته شده کار نمی‌کند — کار می‌کند و آزموده شده. به این دلیل که
کاربر نمی‌تواند کاری را که محصول قول می‌دهد انجام دهد: واحد یک دسته را تعیین کند، محصولی را
برچسب بزند، ضریب تبدیلی ثبت کند، یا تاریخچهٔ قیمتی را ببیند. Backend هر چهار را دارد و
درِ ورودی هیچ‌کدام ساخته نشده.

### Integration Kit / Handoff

**آماده نیست.** دو چیز لازم است:

1. سه رابط پیکربندی (واحد دسته، برچسب محصول، ضریب تبدیل) تا ۸۴٪ کاتالوگ از حالت
   قیمت‌گذاری‌نشدنی بیرون بیاید.
2. یک چرخهٔ فاکتور تأییدشده روی دادهٔ واقعی تا مسیر هزینهٔ واقعی یک بار end-to-end دیده شود.

مهاجرت پایگاه داده اما آماده است: زنجیرهٔ `0001…0025` خطی است، `0025` روی
`bambo_canonical_test` اجرا و راستی‌آزمایی شده، و بستهٔ بازبینی مهاجرت هر ۲۵ revision را
توضیح می‌دهد.
