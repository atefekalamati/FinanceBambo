"""Host-side external price collection adapters.

The deployable Finance domain consumes their provider-neutral results.  Network and parser
choices stay outside ``app.finance`` in the same way as the host-owned extraction adapters.
"""
