# -*- coding: utf-8 -*-
"""TEST_ONLY -- DISPOSABLE. See README.md.

A package only so `python -m scripts.test_only.mpp_parser_probe.load_scratch` resolves.
Nothing outside this directory imports it, and deleting the directory removes it entirely.
"""
